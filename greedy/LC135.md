## 135. Candy

Date: 一刷 4/2026（随想录，日期待补）, 二刷 8/24/2026
Difficulty: Hard
Tags: greedy, array, two-pass

![Two-pass scan: left-to-right enforcing the left neighbor rule, right-to-left taking max to enforce the right neighbor rule without breaking the first pass](assets/135.png)

### 二刷 (8/24/2026) ❌ 看出要扫两趟，代码没写出来，直接看的答案

**卡在哪**：看出要遍历两次，但从「扫两趟」到「每趟各干什么、怎么合并」写不出来，去看了答案，看懂很快。

**真正的坎**：不在「扫几趟」，在于**左右两条规则互相冲突**。每个 element 要同时满足「比左邻分高就比左邻糖多」和「比右邻分高就比右邻糖多」，两条约束方向相反，一趟扫描只能兑现一条。

第二趟的关键不是「反着再来一遍」，而是 `ans[i] = Math.max(ans[i], ans[i+1] + 1)` 里的 **`Math.max`**——直接赋值会推翻第一趟已经建立的左邻关系，取 max 才能让两条约束同时成立。**这一行是不是自己想出来的，8/25 裸写时验。**

**代码里那处错（抄写手误）**：

```java
for(int i = n - 2; n >= 0; i--){   // ❌ 条件写的是 n，应为 i
```

`n` 是长度、恒不变 → 条件永真 → loop 停不下来，`i` 减到 -1 时 `ratings[-1]` 当场 AIOOBE。

> **自检信号**：for 头写完横着读一遍，三段主语必须是同一个 variable。**有没有 break，决定同一个手误是静默还是显性**——本次无 break 所以直接崩；若循环体里有 break，它会替错误的循环条件兜底，部分 case 蒙对，反而更难发现。所以有 break 的 loop 尤其要查这一行。

---

<!-- ↓↓↓ 复习时先自己想一遍，再往下翻看答案 ↓↓↓ -->

### 核心思路（一句话）

**两条方向相反的约束，各扫一趟分别兑现；第二趟用 `Math.max` 合并，保证后一趟不推翻前一趟。**

### 正确写法

```java
public int candy(int[] ratings) {
    int n = ratings.length;
    int[] ans = new int[n];

    // 第一趟：左 → 右，只管「比左邻分高就比左邻多」
    ans[0] = 1;
    for (int i = 1; i < n; i++) {
        ans[i] = (ratings[i] > ratings[i - 1]) ? ans[i - 1] + 1 : 1;  // 不满足就回落到 1
    }

    // 第二趟：右 → 左，兑现「比右邻分高就比右邻多」
    for (int i = n - 2; i >= 0; i--) {                                 // 条件是 i 不是 n
        if (ratings[i] > ratings[i + 1]) {
            ans[i] = Math.max(ans[i], ans[i + 1] + 1);                 // 取 max，不能直接赋值
        }
    }

    int answer = 0;
    for (int num : ans) answer += num;
    return answer;
}
```

三处要点：`ans[0] = 1` 与三元里的 `: 1` 共同保证每人至少一颗；第二趟从 `n - 2` 起手（最后一个没有右邻）；`Math.max` 是两条约束能共存的唯一原因。

### 复杂度分析

**Time: O(n)**：三趟线性扫描。
**Space: O(n)**：`ans` 数组。

对比 brute force（反复扫描直到没有 element 需要调整）：省在「把两条约束按方向拆开，各扫一趟就都满足」，不需要迭代到收敛。
