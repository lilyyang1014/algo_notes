## 12. Integer to Roman

Date: 8/31/2026
Difficulty: Medium
Tags: Greedy, Hash Table, Math

![Greedy subtraction over 13 denominations: repeatedly take the largest value that fits, appending its symbol, until num hits 0](assets/12.png)

### 一刷 (8/31/2026) ❌ 没思路：把 13 的解法搬了过来，两题不是镜像

**卡在哪**：先把 `3749` 拆成 `[3,7,4,9]`，然后不知道拿这个数组干什么。

**真正的坎**：**digits 数组一丢掉位置信息就查不了表了**——那个 3 代表的是 3000，而罗马数字里根本没有「3」这个符号。13 里每个字符的值是固定的（`X` 在哪儿都是 10），所以拆开逐位处理成立；12 里每个数位的值取决于它站在哪一位，拆开就死了。

**这题真正要看清的一句话**：那十三个值（1000/900/500/400/100/90/50/40/10/9/5/4/1）就是罗马数字的**全部合法零件**，任何罗马数字都是它们按从大到小的拼接。看清这句，题目就变成「用给定面额凑金额」——**找零钱问题**。

**与 13 的共同卡点（同一天两道）**：胜负都在「表里放什么」这一步，不在循环怎么写。13 是表建对了没用上（key 类型不匹配），12 是表根本没建对（存了「数字→符号」，可 3、7 这些数字没有符号）。

**代码里的其余错**（编译期两处 + 与 13 同形两处）：

```java
ans = map.get(i+1) - cur;   // ❌① String 减 int，编译不过
ans = map.get(i);           // ❌② 拿循环下标 i 当 key 查，map 里没这些 key → 恒返回 null
                            //    同族 LC13：get 的参数是 Object，含义写错编译器不管
ans = ...                   // ❌③ 赋值覆盖，应为 ans += 追加 —— 今天第二次（LC13 的 ans = 4）
i = i + 2;                  // ❌④ for 头已有 i++，再手动加 → 每轮实际前进 3
                            //    步长不固定该用 while —— 今天第二次（LC13 方案 A）
return s;                   // ❌⑤ 返回了输入不是 ans
```

**③④是今天第二次出现，比单个 bug 值钱**：③④和 LC13 那次「三套方案各写一半」是同一个动作——**两套写法拼在一起，每一半单独看都对**。

**自测点**：把 3749 拆成 `[3,7,4,9]` 之后为什么走不下去？说出那个 3 丢了什么。

---

<!-- ↓↓↓ 复习时先自己想一遍，再往下翻看答案 ↓↓↓ -->

### 核心思路（一句话）

**十三个值就是全部面额，从大到小每次减到不能再减，减一次追加一个符号。**

### 正确写法

```java
int[] values     = {1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1};
String[] symbols = {"M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I"};

StringBuilder sb = new StringBuilder();
for (int i = 0; i < values.length; i++) {
    while (num >= values[i]) {      // while 不是 if：同一面额可连用（MMM、CC）
        sb.append(symbols[i]);
        num -= values[i];
    }
}
return sb.toString();               // sb 不是 String，别忘了转
```

**用两个数组不用 Map**：这题需要**按降序遍历**，HashMap 不保证顺序。数组下标顺序就是贪心顺序，两个数组靠同一个 `i` 对齐。

### 为什么贪心是对的（这一层要能讲出来）

贪心通常有翻车风险——面额 `{1,3,4}` 凑 6，贪心得 4+1+1，最优是 3+3。

12 不会翻车，因为**六个减法形式（900/400/90/40/9/4）已经被当成独立面额放进表里了**。若表里只有 M/D/C/L/X/V/I 七个，凑 9 会得到 `VIIII`——贪心走进死胡同，因为它不知道 `IX` 这条路。把 IX 也列成面额后，9 这一格直接有解，不需要回头。

**贪心之所以成立，不是它聪明，是表被设计成让它不会犯错。**

### 复杂度分析

**Time O(1)**：num ≤ 3999，外层 13 档、内层最多减 3 次，与输入规模无关。
**Space O(1)**：两张表是常数大小。

### 沉淀

- **拆数位之前先问一句：拆完之后每一位还认得出自己是多少吗？** 12 拆完就死了，因为位权丢了
- **需要顺序遍历就别用 Map**，用两个平行数组按同一个 `i` 对齐。「这个容器给不给我顺序」是选容器的第一问
- **同一面额可能连用 → 内层必须是 `while` 不是 `if`**
- **步长不固定就别用 for**：for 的 update statement 写死了，手动改 `i` 会和它叠加（今天两次）
- **`ans = ` 与 `ans += ` 是两件事**：覆盖不是修正（今天两次，另一次在 LC13）
- **贪心正确性的通用问法**：什么情况下"拿最大的"会让后面无解？答不上就是没想清楚，不是"感觉对"

### 引申：什么时候用 StringBuilder

**判据只有一条：拼接动作在不在循环体里。** 在 → StringBuilder；不在 → String 直接 `+` 就行。

**为什么**：String 不可变，`s += c` 不是「往里加」，而是新建一个 String + 复制全部已有字符。单次是 O(当前长度)，放进循环就是 1+2+…+n = **O(n²)**。StringBuilder 内部是可变 `char[]`，append 就是往末尾写一格、满了扩容——**与 ArrayList 完全同构**，摊还 O(1)。

> **一句话记法**：ArrayList 的代价来自「下标必须连续」，**String 的代价来自「不可变」**——任何修改都得复制全体。

**循环外不必纠结**：`"Hello, " + name + "!"` 编译器会自动优化成 StringBuilder。但**这个优化不跨循环边界**，循环里每一轮 `+=` 都是独立的一次拼接，编译器帮不了你。所以分界不是「拼几次」，是「在不在循环里」。

**常见触发场景**：构造答案串（12、38、6、43）；`sb.reverse()` 反转/判回文；回溯的路径（`append` → 递归 → `deleteCharAt(sb.length()-1)` 回退）。

| 调用                 | 返回                   | 备注                                            |
| -------------------- | ---------------------- | ----------------------------------------------- |
| `sb.append(x)`       | **StringBuilder 自己** | 所以能链式 `sb.append(a).append(b)`             |
| `sb.toString()`      | String                 | 最后调一次，这才产生不可变对象                  |
| `sb.reverse()`       | 自己                   | 原地                                            |
| `sb.deleteCharAt(i)` | 自己                   | 回溯标配 `deleteCharAt(sb.length()-1)`          |
| `sb.length()`        | int                    | **不是 `.size()`**，跟 String 一样用 `length()` |
| `sb.setCharAt(i, c)` | void                   | String 没有的能力                               |

`append` 返回自己、`list.add` 返回 boolean、`map.put` 返回旧 value——三个不同的答案，都是「它还给我什么」那一轴。`StringBuffer` 是线程安全版，单线程一律用 StringBuilder。

### 关联

- LC13 Roman to Integer（同一张表的反向查；12 的输出喂给 13 应还原成原数，可互验）
- LC45 / LC55 / LC134 / LC135（同为 greedy，复习时对照「为什么这个 greedy 是对的」这一层）
