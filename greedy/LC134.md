## 134. Gas Station

Date: 一刷 4/3/2026（随想录）, 二刷 8/17/2026, 三刷 8/21/2026
Difficulty: Medium
Tags: greedy, array

![Scanning the circular route: curSum dropping below zero at index i, start jumping to i+1, while totalSum keeps accumulating across the whole array](assets/134.png)

### 二刷 (8/17/2026) ❌ 没思路，读题解后复述有两处偏差

**卡在哪**：只记得「能用 greedy」，过程完全不记得，没独立写出代码，直接看了题解。

**真正的坎**：缺的不是「想到 greedy」，是它的两条正确性依据（有解的判据 + 为什么整段候选能一次跳过）。**看题解觉得容易 = recognition，和能重建出来是两回事**——证据是读完后的自述有两处和代码对不上：

❌① 自述「cur 和 total 都重置为 0」——代码只重置 `curSum`，`totalSum` **绝不能重置**。清零它 = 把前面的亏空抹掉 → 最后必然 ≥ 0 → 无解的 case 也会返回一个 start，静默错误。

❌② 自述两次说「`totalSum > 0` 就 return start」——代码是 `totalSum < 0` 才 return -1，**`== 0` 归有解那边**（总 gas 恰好等于总 cost = 刚好开完一圈，合法）。

**①②是一对**：都是「这个量此刻代表什么 / 边界落在哪一格」。

### 三刷 (8/21/2026) ❌ 三处错，思路在但没落地

```java
for (int i = 0; i < gas.length - 1; i++) {   // ❌③ 上界少一格，最后一站被跳过
    cur = gas[i] - cost[i];                  // ❌❌① 应为 cur += ，覆盖掉了累计
    total += gas[i] - cost[i];               //     ✅ total 这行是对的
    if (cur < 0) {
        start++;                             // ❌② 应为 start = i + 1
        cur = 0;
    }
}
```

**①（核心）**：`cur` 模拟的是**油箱**，油箱本来就有存量，必须累加。写成 `=` 之后 `cur` 只反映单站盈亏，于是 `cur < 0` 判的变成「这一站自己亏」而不是「从 start 走到这撑不住了」——**判断的含义整个变了**，`start` 的更新也就失去了依据。单站盈亏根本不是判据：`gas=[1,2,3,4,5], cost=[3,4,5,1,2]` 里自身不亏的是 3 和 4 两站，正确起点却只有 3。

**②（我自己定位的）**：把数组理解成一个区间——**前面这一段累加的油箱为负了，start 要从 `i+1` 开始算，不能 `++`**。`start++` 是把「第几格」当成「失败了几次」在数，而且丢掉了整段跳过这条剪枝，退化成 O(n²)。

**③**：绕圈问题每一站都要算，没有「最后一站不用管」。`total` 少加一项、`cur` 少走一段。

**①②是连着的**：`cur` 一退化成单站值，重置逻辑就没了依据，`start++` 顺理成章地被写出来。

**自测点**：一趟从左到右的线性 scan，为什么就等价于「绕圈」？`start` 之后那一段（`start` 到末尾）压根没验证过它能不能绕回开头那几站，凭什么直接 return `start`？

---

<!-- ↓↓↓ 复习时先自己想一遍，再往下翻看答案 ↓↓↓ -->

### 核心思路（一句话）

**`totalSum >= 0` 决定「有没有解」，`curSum` 转负的位置决定「解在哪」——一趟 scan 同时算这两件事。**

### 代码

```java
public int canCompleteCircuit(int[] gas, int[] cost) {
    int curSum = 0;      // 从 start 到 i 这一段的净油量（有存量，必须累加）
    int totalSum = 0;    // 全程净油量，判可行性，全程不清零
    int start = 0;
    for (int i = 0; i < gas.length; i++) {
        curSum += gas[i] - cost[i];
        totalSum += gas[i] - cost[i];
        if (curSum < 0) {
            start = i + 1;   // [start, i] 整段作废，i+1 尚未检验
            curSum = 0;      // 只清 curSum
        }
    }
    if (totalSum < 0) return -1;   // == 0 有解，不能写 <= 0
    return start;
}
```

### 推导：为什么整段 `[start, i]` 可以一次跳过

设 `curSum` 在 `i` 处首次转负，取段内任意 `j`：

从 start 出发能走到 j（是走到 i 才断的），说明**开到 j 时油箱里还有富余**。那从 j 自己出发呢？它起步是 0，比「从 start 开过来的那个自己」**少了那份富余**。带着富余的版本走到 i 都断了，油更少的版本只会**更早断**。

所以段内每一格做起点都必定失败 → 整段一次排除，`start` 直接跳到 `i+1`。这就是 O(n²) 压成 O(n) 的地方。

### 复杂度分析

**Time: O(n)**：单趟 scan，`start` 单调右移不回退。
**Space: O(1)**：三个 int。

对比 brute force O(n²)（枚举每个起点各模拟一圈）：省在「一整段候选一次排除」，不是逐个起点重试。

### 沉淀

- **`=` 还是 `+=`，先问「这个量有没有存量」**。凡是「前缀和跌破 0 就重开」的形态（134、53），那个变量都在模拟一个会攒的东西，必然累加。三刷栽在这。
- **两个累加量角色不同就不能同命运**：`totalSum` 答「有没有」、`curSum` 答「在哪」，只有后者随起点重置。判据是「这个量的统计范围是全局还是当前窗口」——全局量出现在重置语句里就是坏味道。
- **`start` 是「第几格」不是「第几次」**：它该被赋值成一个位置（`i + 1`），不该被 `++`。同族：189 的 `n-k`、45 的 `i == count`、367 的 `left`/`left²`。
- **可行性判据的等号方向单独想一遍**：`totalSum == 0` 是「恰好开完」不是「差一点」。凡「总量够不够」型判断，先问 0 归哪边。
- **greedy 跳跃型的通用形态**：证明「区间内每点都不优于端点」→ 整段跳过 → O(n²) 压成 O(n)。134 和 45 共用这个骨架。

### 关联

- LC45 Jump Game II（同为一趟 scan 的 greedy 跳跃，都靠「整段可排除」压复杂度）
- LC55 Jump Game（问「能不能到」，本题问「从哪出发能到」——存在性判据和构造起点是两件事）
- LC53 Maximum Subarray（同样是「累计跌破 0 就重开」，`curSum` 的存量语义完全一致）
- LC238（同族错误：两个 variable 角色混淆）

### Interview pitch (练口述)

> "Brute force would try every station as a start and simulate a full loop — O(n²). The key insight is that two separate questions can be answered in one pass. First, feasibility: if total gas minus total cost is negative, no start works, since any full loop consumes that same total — and exactly zero is still feasible. Second, locating the start: I track a running sum from the current candidate. The moment it goes negative at index i, no station in that whole segment can work — any later station j in it starts without the surplus it would have had arriving from start, so it'd reach i with even less gas. I jump start to i plus one and reset only the running sum, never the total. One pass, O(n) time, O(1) space."
